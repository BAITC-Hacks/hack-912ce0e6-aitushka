# Copy to .env in the repository root. Never commit the real key.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.4-mini
# Maximum paid attempts per backend process, including failed requests.
AI_MAX_REQUESTS=50
# Public dashboard URL for AI POST requests, without a trailing slash.
# AI_ALLOWED_ORIGINS=https://your-instance.example
